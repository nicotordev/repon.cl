{
  "name": "repon.cl",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo repon.cl",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "start": "pnpm -r --parallel run start",
    "frontend": "pnpm --filter frontend dev",
    "api": "pnpm --filter api dev"
  }
}
